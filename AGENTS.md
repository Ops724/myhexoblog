# 项目协作约定

本文件写给所有参与本项目的人（包括 AI 助手）。开工前请先读这里，再读 [PLAN.md](./PLAN.md)。

## 项目是什么

一个基于 Hexo 8 的中英双语静态博客。代码公开托管在 GitHub，真实内容只保存在本地，构建后通过 rsync 发布到阿里云 ECS。

## 技术栈

- Hexo 8：静态站点生成器
- EJS：模板引擎
- 原生 CSS 与原生 JavaScript：不引入前端框架
- Node.js 26：本地开发环境

## 三层职责与边界

Hexo 站点分三层，改动前先判断属于哪一层：

| 层 | 位置 | 职责 |
| --- | --- | --- |
| 内容层 | `source/` | 文章、页面、站点数据（`_data/`） |
| 路由层 | 根 `_config.yml`、`scripts/` | 生成哪些 URL、每个 URL 放哪些数据 |
| 渲染层 | `themes/ops724-white/` | 模板、样式、界面文案、浏览器端交互 |

约定：

- 路由与内容规则放在站点层（根 `scripts/`），主题只负责渲染。
- 主题目录保持干净，不写入站点专属的内容判断逻辑。
- Hexo 的生成器按名字注册，同名会覆盖；本项目用站点 `scripts/generators/` 覆盖官方 `index` 生成器。

## 公开代码与私有内容

- 公开：框架、主题、文档、`examples/` 中的示例内容。
- 私有：真实文章、个人图片、关于页、`source/_data/profile.yml`、`source/_data/social.yml`，由 `.gitignore` 排除。
- 初始化本地示例内容：`npm run content:init`（只复制，不覆盖）。
- 每次提交前用 `git status` 检查，确认没有把私有内容加入暂存区。
- 私有内容不在 Git 中，没有版本保护，需自行备份。

## 提交规范

- **所有提交信息一律使用中文。**
- 一次提交只做一件事，按阶段提交，不把多个功能混在一起。
- 使用下面的结构写提交信息，尽量详细，便于日后当学习材料复盘：

```text
<类型>: <一句话说明这次改动>

背景
- 为什么做这次改动，解决什么问题。

改动
- 文件路径：这个文件改了什么，目的和思路是什么。
- 文件路径：...

验证
- 用了什么命令、检查了什么，结果如何。

后续
- 下一步计划，或已知限制。
```

- 类型使用中文：`初始化`、`新增`、`重构`、`修复`、`样式`、`文档`、`构建`。

## 分支模型

- `main`：始终保持可构建、可部署。
- 每个阶段开一条短生命周期分支（例如 `feat/theme-skeleton`），分支名用英文小写短横线。
- 阶段完成后用 `git merge --no-ff` 合并回 `main`，保留一个阶段一个合并节点。

## 常用命令

```bash
npm install           # 安装依赖
npm run content:init  # 首次使用：初始化本地示例内容
npm run server        # 本地预览 http://localhost:4000
npm run build         # 生成静态站点到 public/
```

## 文档

- [PLAN.md](./PLAN.md)：方案书与分阶段 TODO，是设计与进度的基线。
- [docs/COMMITS.md](./docs/COMMITS.md)：提交学习索引，每完成一个阶段追加一行。
- 文档与代码不一致时，先改文档，再改代码。
