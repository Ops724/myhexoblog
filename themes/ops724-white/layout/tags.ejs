<section class="content-section">
  <header class="section-heading">
    <h1><%= page.title %></h1>
    <p class="section-copy"><%= __('ui.tags_intro') %></p>
  </header>
  <%- partial('partials/taxonomy-list', { items: taxonomy_items('tags', page_lang(page)) }) %>
</section>
