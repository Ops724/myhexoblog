# 示例内容

这里存放公开的示例内容，让新克隆的仓库能立刻跑起来。

- `examples/source/` 是 `source/` 的内容模板，目录结构与真实内容保持一致。
- 首次使用执行 `npm run content:init`，脚本会把这里的文件复制到 `source/` 对应位置。
- 脚本只复制、不覆盖：`source/` 中已存在的文件不会被改动，所以不会冲掉真实内容。
- 真实内容不会进入 Git（已被 `.gitignore` 排除），示例内容会。
